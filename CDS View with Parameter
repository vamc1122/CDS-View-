@AbapCatalog.sqlViewName: 'ZPARAMETER'
@AbapCatalog.compiler.compareFilter: true
@AbapCatalog.preserveKey: true
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'with parameter'
define view zvamsi4 with parameters p_mtype : mtart , p_mgroup : matkl
as select from mara
{
   key matnr,
       mtart,
       matkl,
       ntgew
        
}where mtart = :p_mtype 
and matkl = :p_mgroup
